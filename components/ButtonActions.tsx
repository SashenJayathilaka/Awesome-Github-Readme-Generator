"use client";

type Props = {
  setIsShow: (value: boolean) => void;
};

function ButtonActions({}: Props) {
  return <div className="flex justify-between items-center py-2.5"></div>;
}

export default ButtonActions;
